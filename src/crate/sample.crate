$ Let's hope it works!
$ By the way, some of the keywords (like assert) will come later, due to more complex parsing functions involved

$ noeol statements
import mod1; import mod2; package name;

$ null statement
;

$ identifier
random_id;

$ constant
RANDOM_CT;

$ imports
import module;
import MODULE;

$ packages
package mypack;
package MYPACK;
