$ Let's hope it works!
$ By the way, some of the keywords (like assert) will come later, due to more complex parsing functions involved
$ Also, there are no more constant identifiers. I got rid of them from the lexer.

$ null statement
;

$ identifier
random_id;

$ import
import module;

$ package
package mypack;

$ from import
from module import thing;

$ as imports
import module as mod;
from module import thing as th;
